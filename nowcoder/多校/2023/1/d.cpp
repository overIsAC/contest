#include <bits/stdc++.h>
using namespace std;
using LL = long long;
using PII = pair<int, int>;

int main() {
    int n, m;
    cin >> n >> m;
    if (n == 1 && m == 1) {
        cout << "Walk Alone" << endl;
    } else {
        cout << "Kelin" << endl;
    }
    return 0;
}